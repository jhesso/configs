to get plugins working.. make sure to install tpm:
```bash
# clone TPM
git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm
# Source tmux config if tmux is open
tmux source ~/.config/tmux/tmux.conf
```
